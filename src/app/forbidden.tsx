// Rendered by forbidden() calls (src/lib/auth-guards.ts). Placeholder only —
// real styling/content is M3.
export default function Forbidden() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center gap-sm p-xl text-center">
      <h1 className="font-headline-lg text-headline-lg text-on-surface">403 — Forbidden</h1>
      <p className="font-body-md text-body-md text-on-surface-variant">
        You don&apos;t have access to this page.
      </p>
    </div>
  );
}
